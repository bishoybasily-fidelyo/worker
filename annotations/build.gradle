apply plugin: 'java'
apply plugin: 'maven'

group = 'com.fidelyo.worker.annotations'
version = '1.0.1'

dependencies {
    compile fileTree(dir: 'libs', include: ['*.jar'])
}

uploadArchives {
    repositories {
        mavenDeployer {
            mavenLocal()
        }
    }
}
