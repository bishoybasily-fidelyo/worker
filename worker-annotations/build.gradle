apply plugin: 'java'
apply plugin: 'maven'

group = 'com.fidelyo.worker'
version = '1.0'

dependencies {
    compile fileTree(dir: 'libs', include: ['*.jar'])
}

uploadArchives {
    repositories {
        mavenDeployer {
            mavenLocal()
        }
    }
}
